# render.yaml — Configuration de déploiement Render pour PAP-LFI
services:
  - type: web
    name: pap-lfi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
